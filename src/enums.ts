export enum FILE_NAMES {
  PHRASES = 'phrases.json',
}

export enum THEME {
  LIGHT = 'LIGHT',
  DARK = 'DARK',
}

export enum SETTING_KEYS {
  TOKEN = 'LP2_TOKEN',
  GIST_ID = 'LP2_GIST_ID',
  PREFERRED_THEME = 'PREFERRED_THEME',
}
